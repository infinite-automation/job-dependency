name: job dependency demo

on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build
        run: echo "Building the project..."

  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test
        run: echo "Running tests..."
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy
        run: echo "Deploying the project..."